*Special_chars*

# has to be escaped!!!! \#
% same
after ~ in command mode devi espanderla alla home se la vuoi usare come home

*other*

:put reg

:grepadd, :cfdo (per file)

:sort /pattern/, fa sort su tutto cio' che viene dopo pattern
:sort r /pattern/, o :sort /pattern/ r fa sort su pattern

" vim:ft=help
